import express from 'express';
import mandrill from 'mandrill-api/mandrill';
import { Router } from 'express';
import dotenv from 'dotenv';

// Restante do código...

// Altere também a exportação:
// module.exports = router;
// Para:
export default router; 